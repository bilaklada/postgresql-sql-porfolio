# ERD Assets

This folder is reserved for exported entity relationship diagrams of the
`dvdrental` schema.

The current repository explains relationships in
`docs/02_schema_overview.md` and `docs/03_relational_model_notes.md`, so an ERD
image is useful but not required for the SQL to be reviewable.
